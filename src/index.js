import React from "react";
import FullPage from "./Home/FullPage";

const TypeformReactjs = props => {
  return (
    <FullPage data={props.data} url={props.url} />
  );
};

export default TypeformReactjs;